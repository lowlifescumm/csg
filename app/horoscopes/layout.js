export const metadata = {
  title: "Daily Horoscopes | Cosmic Spirit Guide",
  description: "Free daily horoscopes for all zodiac signs",
};

export default function HoroscopesLayout({ children }) {
  return children;
}
